export default function ApiHomePage() {
  // This screen is intentionally simple for the first commit.
  // It confirms the backend workspace boots independently.
  return (
    <main style={{ fontFamily: "sans-serif", padding: "2rem" }}>
      <h1>RandevuMind API</h1>
      <p>Backend workspace is ready for appointment and operations services.</p>
    </main>
  );
}
